kind: Service
apiVersion: v1
metadata:
  name: kubernetes-configmap.yml
spec:
  type: LoadBalancer
  selector:
    app: kubernetes-configmap.yml
  ports:
    - name: http
      protocol: TCP
      # ELB's port
      port: 8081
